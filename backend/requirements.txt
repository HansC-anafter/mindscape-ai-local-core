# Core web framework
fastapi==0.104.1
uvicorn[standard]==0.24.0

# Data validation and serialization
pydantic==2.5.0
pydantic-settings==2.1.0

# Tool Ecosystem (Phase 1+)
# LangChain 1.0+ support (optional - install with: pip install ".[langchain]")
# langchain>=0.1.0
# langchain-core>=0.1.0
# langchain-community>=0.0.20

# Security and encryption (for v1+)
cryptography==41.0.7
python-jose[cryptography]==3.3.0
bcrypt==4.0.1
python-multipart==0.0.6

# LLM Providers
openai>=1.54.0
anthropic==0.7.8
google-cloud-aiplatform>=1.38.0
vertexai>=1.38.0
google-auth>=2.23.0

# Database (SQLite is built-in, but we may add async support later)
aiosqlite==0.19.0
psycopg2-binary==2.9.9
pgvector==0.2.4

# Database migrations
alembic==1.13.1

# Utilities
python-dotenv==1.0.0
requests==2.31.0
aiohttp==3.9.1
tiktoken==0.5.2

# Development and testing
pytest==7.4.3
pytest-asyncio==0.21.1
httpx==0.28.1

# Logging and monitoring
structlog==23.2.0

# YAML parsing for Playbook frontmatter
pyyaml==6.0.1

# PDF text extraction
PyPDF2==3.0.1

# File locking for cross-platform atomic writes
portalocker>=2.0.0
redis>=4.0.0

# ⚠️ Hard Rule: local-core core does NOT depend on cloud contract packages
# sitehub-execution-protocol is only installed in pack runtime venv (via Pack Installer)
# For cloud handoff, local-core sends JSON payload to site-hub API (no protocol package needed)
