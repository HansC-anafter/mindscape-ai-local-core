"""Legacy bootstrap helpers retired in favor of declarative resolver rules.

Meeting-core bootstrap defaults are now loaded from playbook specs and tool
manifests via generic resolver names in
``backend.app.services.orchestration.default_input_resolvers``.
"""
