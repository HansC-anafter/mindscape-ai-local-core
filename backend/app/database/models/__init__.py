"""Database models package."""

# Stub: UnsplashPhotoFingerprint is expected by unsplash.fingerprint_tools
# but the actual ORM model hasn't been created yet.
# This stub prevents ImportError during capability loading.


class UnsplashPhotoFingerprint:
    """Non-ORM stub — prevents ImportError in fingerprint_tools.
    The actual table/model does not exist yet."""
    __tablename__ = "unsplash_photo_fingerprints"
