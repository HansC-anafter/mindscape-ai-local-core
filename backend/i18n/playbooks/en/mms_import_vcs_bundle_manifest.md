## Import VCS Bundle Manifest

This playbook reads VCS `manifest.json` and imports chapters into an MMS `chapter` track as timeline items.

### Inputs

- `project_id` (MMS project)
- `manifest_ref` (source: `url` / `file_path` / inline `manifest`)

